This application is a simple form which enter the user's data to the database and perform the CRUD operation on them.
HTML, CSS, JS is use for front end. 
C# is used for backend.
.NET framework is used.
Develop in VS
